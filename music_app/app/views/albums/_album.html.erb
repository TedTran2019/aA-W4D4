  <tr>
    <td><%= link_to "#{album.title}", album_url(album) %></td>
    <td><%= link_to "#{album.band.name}", band_url(album.band_id) %></td>
    <td><%= album.year %></td>
		<td><%= album.live %></td>
  </tr>
